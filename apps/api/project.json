{
  "name": "api",
  "sourceRoot": "apps/api/src",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config apps/api/jest.config.js --runInBand"
      }
    }
  }
}