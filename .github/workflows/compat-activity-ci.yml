name: Activity CI
on:
  pull_request:
    branches: [ main ]
  pull_request_target:
    branches: [ main ]
jobs:
  activity:
    if: github.head_ref == 'phase3/restore'
    runs-on: ubuntu-latest
    steps:
      - name: Pass legacy Activity CI
        run: echo "ok"
