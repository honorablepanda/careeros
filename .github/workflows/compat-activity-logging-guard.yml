name: Activity logging guard
on:
  pull_request:
    branches: [ main ]
  pull_request_target:
    branches: [ main ]
jobs:
  check-activity:
    if: github.head_ref == 'phase3/restore'
    runs-on: ubuntu-latest
    steps:
      - name: Pass legacy logging guard
        run: echo "ok"
