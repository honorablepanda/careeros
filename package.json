{
  "name": "careeros",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "prepare": "husky install",
    "test:api": "pnpm -w jest --clearCache --config apps/api/jest.config.ts && pnpm -w exec nx run api:test --verbose",
    "test:api:guarded": "node tools/scripts/guard-jest-config.cjs && pnpm run test:api",
    "test": "echo No root tests; use pnpm run test:api"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "husky": "^8.0.0",
    "jest": "30.1.3",
    "ts-jest": "29.4.1",
    "typescript": "5.9.2"
  }
}